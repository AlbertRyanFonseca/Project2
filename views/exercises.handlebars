<section class="container">
    <div class="row ">
        {{#each posts as |post|}}
        <div class="col-md-3">
            {{> exercise-info post}}
        </div>
        {{/each}}
    </div>
</section>