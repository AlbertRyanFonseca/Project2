{{> filter-bar }}
<section class="container">
    <div class="row mt-3">
        {{#each posts as |post|}}
        <div class="col-md-3 mt-3">
            {{> exercise-info post}}
        </div>
        {{/each}}
    </div>
</section>
