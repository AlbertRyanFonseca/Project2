{{> filter-bar }}
<section class="container">
    <div class="row mt-3 postId">
        {{#each posts as |post|}}
        <div class="col-md-3 mt-3">
            {{> exercise-info post}}
        </div>
        {{/each}}
    </div>
</section>

<script src="javascript/post-card.js"></script>
<script src="javascript/filter-bar.js"></script>
